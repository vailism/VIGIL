services:
  - type: web
    name: sanket-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn sanket.api:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: GEMINI_API_KEY
        sync: false
